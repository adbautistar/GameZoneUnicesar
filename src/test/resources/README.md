# Test Resources

Placeholder for test fixture resources (e.g., sample CSV files) used by the
automated test suite under `src/test/java`. Currently empty: all test data is
generated programmatically via `com.gamezone.testutil.TestDataFactory` and
isolated with `com.gamezone.testutil.TempFileHelper`.
